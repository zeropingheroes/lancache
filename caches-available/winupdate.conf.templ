server {
    listen *;

    access_log $CACHE_LOGS_DIRECTORY/winupdate-access.log cache-key-uri-slice-range buffer=128k flush=5s;
    error_log $CACHE_LOGS_DIRECTORY/winupdate-error.log;

    include upstreams/winupdate.conf;
}
